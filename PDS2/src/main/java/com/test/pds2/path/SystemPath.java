package com.test.pds2.path;

import java.io.File;
import javax.servlet.http.HttpSession;

public class SystemPath {
	public static final String SYSTEM_PATH = "D:" + File.separator + "upload" + File.separator;
	//public static HttpSession SESSION = new HttpSession();
	//public static final String SYSTEM_PATH = new HttpSession().getServletContext().getRealPath(File.separator + "resources" + File.separator + "upload" + File.separator);
	
	
}
